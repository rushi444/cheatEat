generator prisma_client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "postgres"
    url = "***"
}

model User {
    id   Int    @id @default(autoincrement())
    name String
}
